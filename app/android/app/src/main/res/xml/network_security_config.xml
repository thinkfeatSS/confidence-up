<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Strictly block all unencrypted cleartext HTTP traffic across the app -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
